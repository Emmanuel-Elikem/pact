# Pact Implementation Plan

> **Sole implementer — execute inline.** TDD only for Solidity forge tests.

**Goal:** Ship Pact hackathon demo: Vite UI + Foundry CampaignEscrow/MockUSDC + WAAP/demo wallet + mock TikTok.

**Architecture:** Monorepo at `/home/demigod/PROJECTS/pact`. `apps/web` talks to Anvil/Base Sepolia via ethers v6.

**Tech Stack:** Foundry, OpenZeppelin, Vite, React 19, TS, Tailwind v4, React Router, lucide-react, framer-motion, ethers v6, `@human.tech/waap-sdk`

**Tagline:** Campaign funds locked until creators deliver.

---

### Task 1: Contracts
- [ ] MockUSDC + CampaignEscrow + forge tests + Deploy.s.sol
- [ ] `forge test` green

### Task 2: Web app
- [ ] Vite React TS + design tokens + all screens
- [ ] Wallet (WAAP + demo), escrow flows, mock TikTok
- [ ] `npm run build` green

### Task 3: Ship
- [ ] Anvil deploy smoke, README, GitHub `pact`, Vercel attempt
