# Dependencies
node_modules/
dist/
dist-ssr/
*.local
.env
.env.*
!.env.example

# Foundry
contracts/cache/
contracts/out/
contracts/broadcast/

# Keep lib locally; prefer forge install on clone if missing
# contracts/lib/ is committed without nested .git for hackathon portability

# OS / IDE
.DS_Store
.idea/
.vscode/
*.log
.vercel

# Installed via forge install / setup script
contracts/lib/
